<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
	<TargetFrameworks>net35;net4;net45;netstandard1.4;netstandard2.1</TargetFrameworks>
    <ProjectGuid>{5E240B61-4CCD-4C2A-A0F2-F545C5E9939C}</ProjectGuid>
    <OutputType>Library</OutputType>
    <RootNamespace>Murmur</RootNamespace>
    <AssemblyName>MurmurHash</AssemblyName>
	  <AllowUnsafeBlocks>True</AllowUnsafeBlocks>
	  <Version>1.0.0</Version>
	  <Title>MurmurHash</Title>
	  <Product>MurmurHash</Product>
	  <Copyright>Copyright ©  2012</Copyright>
  </PropertyGroup>
  <!-- To modify your build process, add your task inside one of the targets below and uncomment it.
       Other similar extension points exist, see Microsoft.Common.targets.
  <Target Name="BeforeBuild">
  </Target>
  <Target Name="AfterBuild">
  </Target>
  -->
</Project>